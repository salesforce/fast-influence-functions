apiVersion: v1
kind: Pod
metadata:
 name: sfr-pod-hguo
 namespace: sfr-ns-hguo
spec:
  restartPolicy: OnFailure
  volumes:
    - name: sfr-home-pv-hguo
      persistentVolumeClaim:
        claimName: sfr-home-pvc-hguo
    - name: sfr-share-pv-hguo
      persistentVolumeClaim:
        claimName: sfr-share-pvc-hguo
  containers:
    - name: hguo-scratchpad
      image: "gcr.io/salesforce-research-internal/hguo-scratchpad:IMAGE_TAG"
      command: ["/bin/bash"]
      args: ["-c", "cd hguo-scratchpad/ && bash ./scripts/run_experiments.sh"]
      resources:
        limits:
          nvidia.com/gpu: 1
          cpu: "7"
          memory: 45G
      volumeMounts:
        - name: sfr-home-pv-hguo
          mountPath: "/export/home"
        - name: sfr-share-pv-hguo
          mountPath: "/export/share"
  tolerations:
  - key: "gpu_num"
    operator: "Equal"
    value: "2"
    effect: "NoSchedule"